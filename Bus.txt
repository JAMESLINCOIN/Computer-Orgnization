总线（Bus）：硬件组件的 “通信高速公路”
总线是连接 CPU、内存、I/O 设备的一组公共导线，负责在各组件间传输数据、地址和控制信号，避免组件间 “一对一” 连接导致的线路混乱。

按连接部件不同，可分为三类：
1.片内总线：芯片内部的总线，如cpu芯片内部、寄存器与寄存器之间
2.系统总线：系统总线是指CPU、主存、1/0设备（通过1/0接口）各大部件之间的信息传输线，又称板级总线或板间总线
3.通信总线：用千计算机系统之间或计算机系统与其他系统（如控制仪表、移动通信等）之间的通信，按传输方式可分为串行通信和并行通信

系统总线按传输信号类型可分为三类：
地址总线（AB）：CPU 向内存 / 外存发送 “数据存储地址”（如 “读取内存中地址为 0x1234 的数据”），地址总线的宽度（如 32 位、64 位）决定了 CPU 可访问的最大内存容量（32 位总线最大支持 4GB 内存，64 位支持千亿 GB）。
数据总线（DB）：传输指令和数据（双向传输，如 CPU 向内存写数据、内存向 CPU 读数据），数据总线的宽度（如 32 位、64 位）决定了单次传输的数据量（64 位总线单次可传 8 字节，比 32 位快一倍）。
控制总线（CB）：传输控制信号（如 CPU 向内存发 “读 / 写命令”，I/O 设备向 CPU 发 “中断请求”），协调各组件的工作时序。


总线标准(主流)
内部总线：
PCIe
外部总线：
存储器：SATA,NVMe,SAS
通用外设：USB
显示总线：HDMI,DP,VGA